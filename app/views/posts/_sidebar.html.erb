<div class="card">
  <div class="card-body">
    <div><span class="h6">Who to follow</span> .
      <span class="small"><a href="#" style="color: #009688;">Refresh</a> · <a href="#" style="color: #009688;">View all</a></span>
    </div>
  </div>
  <ul class="list-group list-group-flush">
    <% user.who_to_follow.each do |u| %>
      <%= render 'who_to_follow', user_to_follow: u %>
    <% end %>
  </ul>
</div>

<div class="card mt-3">
  <div class="card-body">
    © 2018 Mayowa Yakub
  </div>
</div>
